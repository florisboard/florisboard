<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission needed to vibrate if the user has key press vibration enabled -->
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Permission needed to create notifications on devices running Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- Permission needed to record audio for voice input -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Permission needed to connect to the internet for voice input -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Android 11+ only: Define that OmniBoard requests to see all apps that
          ship with an IME or Spell Check service. This is used to guide the user
          in the Settings Ui why OmniBoard may not be working.
     -->
    <queries>
        <intent>
            <action android:name="android.view.InputMethod"/>
        </intent>
        <intent>
            <action android:name="android.service.textservice.SpellCheckerService"/>
        </intent>
    </queries>

    <application
        android:name="dev.silo.omniboard.OmniApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/backup_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/omni_icon"
        android:label="@string/omni_app_name"
        android:enableOnBackInvokedCallback="true"
        android:roundIcon="@drawable/omni_icon"
        android:supportsRtl="true"
        android:theme="@style/OmniAppTheme"
        tools:targetApi="tiramisu">

        <!-- Allow app to be profiled for benchmarking and baseline profile generation -->
        <profileable android:shell="true"/>

        <!-- IME service -->
        <service
            android:name="dev.silo.omniboard.OmniImeService"
            android:label="@string/omni_app_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:directBootAware="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod"/>
            </intent-filter>
            <meta-data android:name="android.view.im" android:resource="@xml/method"/>
        </service>

        <!-- Spellchecker service -->
        <service
            android:name="dev.silo.omniboard.OmniSpellCheckerService"
            android:label="@string/omni_app_name"
            android:permission="android.permission.BIND_TEXT_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.textservice.SpellCheckerService"/>
            </intent-filter>
            <meta-data android:name="android.view.textservice.scs" android:resource="@xml/spellchecker"/>
        </service>

        <!-- Main App Activity -->
        <activity
            android:name="dev.silo.omniboard.app.OmniAppActivity"
            android:icon="@drawable/omni_icon"
            android:label="@string/settings__title"
            android:launchMode="singleTask"
            android:roundIcon="@drawable/omni_icon"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/OmniAppTheme.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ui" android:host="omniboard" android:pathPrefix="/" />
            </intent-filter>
            <intent-filter android:label="Import Extension">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="content"/>
                <data android:mimeType="application/vnd.omniboard.extension+zip"/>
                <data android:mimeType="application/octet-stream"/><!-- Firefox looking at you :eyes: -->
            </intent-filter>
            <intent-filter android:label="Import Extension">
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="application/vnd.omniboard.extension+zip"/>
                <data android:mimeType="application/octet-stream"/><!-- Firefox looking at you :eyes: -->
            </intent-filter>
        </activity>

        <!-- Using an activity alias to disable/enable the app icon in the launcher -->
        <activity-alias
            android:name="dev.silo.omniboard.SettingsLauncherAlias"
            android:icon="@drawable/omni_icon"
            android:label="@string/omni_app_name"
            android:launchMode="singleTask"
            android:roundIcon="@drawable/omni_icon"
            android:targetActivity="dev.silo.omniboard.app.OmniAppActivity"
            android:theme="@style/OmniAppTheme.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity-alias>

        <!-- Crash Dialog Activity -->
        <activity
            android:name="dev.silo.omniboard.lib.crashutility.CrashDialogActivity"
            android:icon="@drawable/omni_icon"
            android:label="@string/crash_dialog__title"
            android:configChanges="orientation|screenSize"
            android:theme="@style/CrashDialogTheme"/>

        <!-- Copy to Clipboard Activity -->
        <activity
            android:name="dev.silo.omniboard.ime.clipboard.OmniCopyToClipboardActivity"
            android:theme="@style/OmniAppTheme.Transparent"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="image/*"/>
            </intent-filter>
        </activity>

        <!-- Clipboard Media File Provider -->
        <provider
            android:name="dev.silo.omniboard.ime.clipboard.provider.ClipboardMediaProvider"
            android:authorities="${applicationId}.provider.clipboard"
            android:grantUriPermissions="true"
            android:exported="false">
        </provider>

        <!-- Default file provider to share files from the "files" or "cache" dir -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider.file"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

        <!-- Disable default EmojiCompat initializer -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                tools:node="remove"/>
        </provider>

    </application>

</manifest>
